import React from 'react'
import styled from 'styled-components'

const Hand = props => (
    <div>
        Hello World!
    </div>
)

// {props.hand.forEach(card => {
//     <Card />
// })}

export default Hand
